# 全施設・開閉店記事の一括分類（2026-09-25）

## 現在地

- 元7,764件＋追加済み1件＝7,765施設を全件台帳化し、19市町へ機械的に振り分けた。位置・営業状態の確認済みという意味ではない。
- 5サイトから**1,186記事**を公開APIで一括取得。URL・掲載日時・取得日時・本文・外部リンク・原取得ページ・ハッシュを保存。分類入力は機械作成で、人が先に結論を付けた要約ではない。
- 保存済み採否・保留69判断行（施設IDへの参照59施設）を再利用。GPTChatの2,472件版は未回収で、加算していない。
- **Jevで1,186記事・106通信の一括分類が完了。** ユーザーが最大3,000対象・$1・既存クレジットのみを承認し、承認記録を保存。入力1,321,453 tokens、出力109,650 tokens、使用量ベース推定$0.055501026。API応答読込合計57.655秒、実行全体61.917秒（収集・実装・保存時間は含まない）。購入・自動チャージ変更なし。
- 今回の地図変更0。前段の4施設変更＋大丸の予定表示は維持。ここでの成果は全件台帳・大量取得・照合候補・分類入力。

## 回収結果

| 公開カテゴリ | 記事数 |
| --- | ---: |
| 号外NET 宇部市・山陽小野田市 | 103 |
| 号外NET 山口市・防府市 | 119 |
| 号外NET 周南市・下松市・光市 | 583 |
| 号外NET 下関市 | 117 |
| 開店閉店.com 山口 | 264 |
| 合計 | 1,186 |

API掲載日時に `after=2024-01-01T00:00:00`、`before=2026-09-25T09:00:00` を固定。15ページを取得し、各APIの期間内総件数と一致。カテゴリ外、2024年より前の記事の更新、SNSのみの告知、全県全店の網羅は保証しない。URL・カテゴリIDは `data-sources/facility-bulk-triage-20260925/sources.json`。[WordPress公式API](https://developer.wordpress.org/rest-api/reference/posts/)を使用。

## 照合候補（確定ではない）

名称一候補28記事、複数候補3、市町不整合22、名称候補なし1,133。市町不整合を除く候補が付いた施設は29。**29件だけを処理する配分ではなく、全1,186記事をJevへ送る。** 名称候補なしは未登録新店・表記差・店名抽出不足などを含む。

当初の広い部分一致は107施設だったが、テナント名内の商業施設等を同じ店舗として拾う雑音があったため別欄へ分離した。残る候補も住所・電話・運営者による詳細照合が必要。

施設の主経路は記事候補29、ブランド一覧候補613、地域記事・店舗資料736、公式一覧6,318、既存保留52、以前採用済み3、参照用14。排他的な作業経路であり、確定した営業状態や施設種別ではない。

## Jev分類結果と次の確認経路

開店568、閉店198、移転・改称33、休業・改装53、複数変更320、対象外14。合計1,186は記事数であって店舗数ではない。別記事で同じ店を扱う場合がある。予定の告知も含む。対象外14も削除せず台帳に残す。

確認経路は、名称未照合の探索683記事、低信頼・複数イベント474、単一名称候補の店舗同一性・一次資料確認12、市町不整合8、複数ID候補3、対象外のサンプル確認6。低信頼には対象外ラベルの8記事を含むため、対象外14とサンプル経路6は矛盾しない。

全原応答のモデル・回答ID・確率分布・件数・使用量・ハッシュを検査した。完了後の再実行では106通信がすべてスキップされ、追加送信0を実測。分類の正答率は未測定で、人が全記事を読み終えたという意味ではない。

## Jevの方式・費用

`jev-1.13.0` 固定。1記事1問・1通信最大12記事。長文は先頭1,100文字＋末尾300文字と省略表示。画像・リンク先は未読。原本文は非公開保存し、Gitへ転載しない。分類は記事の変更種類だけで、現実の営業状況・同一店舗・発生日を確定しない。

[公式仕様](https://docs.typesafe.ai/models)を2026-09-25に確認。入力100万tokensあたり$0.042、出力無料。1通信65,536入力tokens相当を保守的に予約し、実行106通信の予約額は$0.291766272。実使用量ベースの費用は上記$0.055501026。請求明細との突合は未実施。[複数質問方式](https://docs.typesafe.ai/cookbooks/parallel_questions)を使用。本記事群の分類精度は未測定。

## 再開・再現

リポジトリroot、Node.js 22.12以上で実行する。

```powershell
node scripts/collect-facility-bulk.mjs       # 保存済みページは再取得しない
node scripts/prepare-facility-bulk.mjs      # オフライン台帳・入力生成
node scripts/run-facility-bulk.mjs --dry-run # 送信なし
node --test scripts/test-facility-bulk.mjs scripts/test-jev-batch.mjs
```

承認は `data-sources/facility-bulk-triage-20260925/authorization.json` に記録済み。今回の対象manifestへ結び付けている。既存Windows DPAPI資格情報を実ユーザーで読む。秘密はDrive・Gitへ保存しない。追加入力に進む場合は承認全体の3,000対象・$1を既実行分と合算管理し、バッチごとに予算が復活したと扱わない。

```powershell
node scripts/run-facility-bulk.mjs --run    # 承認・ハッシュ検査、完了通信スキップ
node scripts/report-facility-bulk.mjs       # 生応答の整合性検査、全件順序付きCSV
```

原取得資料・台帳・Jev入力は `outputs/facility-bulk-triage-20260925/`。新しい取得時点や対象変更は別バッチへ保存し、今回の回答済み入力を変更しない。[全件処理順](FACILITY-REVIEW-ORDER.md)に従う。

オフライン24テストと `npm run validate` 成功。中断再開・重複送信防止・承認ハッシュ・費用制限・テナント誤照合・全入力の一意性・決定的並び順を検査。ライブ分類1,186件の結果は別途原応答で検証した。

## 保存先

[Drive：施設全件一括分類_20260925](https://drive.google.com/drive/folders/1DlTaikSVZErwt0ZEY0NacKg0CIc2zDGb)。原資料・CSV/JSON台帳・コードを非公開の再現用ZIPへ保存。公開PRにはコード・取得条件・集計・ハッシュ・この報告のみを置く。
