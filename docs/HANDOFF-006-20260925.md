# 006 引継ぎ正本 — 2026-09-25

## 移行方式の訂正（この節が優先）

最初の006は履歴ごとのforkで文脈容量を消費したため不採用。`01a0d712-1372-7502-a896-573bdbb2c3f4` は「006旧・履歴引継ぎ版（使用しない）」へ改名しアーカイブ済み。ユーザーは削除を希望しているが、利用可能な操作に完全削除がないため削除未了。005と作業ファイルは変更・削除しない。

修正版006は同プロジェクトで**履歴を継承しない新規作成**を使用し、短い要約だけを渡した。初回は要約受領・待機のみ。旧タスクの履歴や本書全文を初めから読み込まない。新規タスクのworktreeには今回の未コミット差分はないため、下記の旧作業場所を正本として確認する。旧作業場所の保全と、会話履歴を引き継がないことは別。

作成受付ID：`client-new-thread:e8140bc3-01d0-49b6-8831-11bc9d5c5dda`。受付時は準備中であり、受付のみで移行完了とは扱わない。

## 最初にすること

この文書と `NEXT-CHAT-START-HERE.md` の先頭だけを読み、作業場所・差分の有無・残件を確認してユーザーに短く報告する。移行中に調査・Jev実行・GitHub更新を再開しない。モデルとeffortを勝手に変えない。以降はユーザーの再開指示に従う。

実装の正本は以下の旧作業場所に保持されている。ファイル移動・削除・全コピー・過去Jevの再実行は不要。新規タスク自身のworktreeとは区別する。

- 実作業場所：`C:\Users\user\.codex\worktrees\420a\バス停コンパクトタウン｜新規開発`
- プロジェクト：バス停コンパクトタウン｜新規開発
- ブランチ：`codex/facility-freshness-news-20260924`
- ベースHEAD：`177fa13942e7743568039e098e2e23ca2450e3a3`
- 継続PR：https://github.com/yyy-yuichi/bus-stop-compact-town/pull/14
- 今回9件の差分は**未コミット・未push**。前段HEADのCI成功と今回差分を混同しない。
- 既存の未追跡 `docs/facility-update-handoff-20260925.html` はユーザー側のファイル。変更・削除・一括ステージ禁止。

## ユーザーの目的と停止理由

閉店・開店・移転を含む施設の鮮度を更新し、特に個人店舗を取りこぼさない。記事を先にJevで一括分類し、同じ地域・公式一覧・運営元を自然なまとまりとして詳細照合する。少数ずつの固定配分、初めからの再調査、リンク発見だけの完了扱いは不可。確定した変更は検証、PR #14、Drive保存・読み戻しまで行う。マージ・公開・購入・自動チャージは禁止。

今回はユーザーの「006へ移行」依頼で停止。直前にPCのメモリ／ページングファイル不足でViteの画面検証が中断された。Chatを変えるだけでPCの不足が解消したとは扱わない。重い並列処理やタブ追加は避ける。ユーザーの他プロセスを勝手に終了したり、ファイルを削除しない。

## 今回の実変更

| 対象 | 判断と反映 |
| --- | --- |
| 宇部市のWatts全8店 | 現行の公式市内一覧を全件照合し、店内売場として8施設を追加。入居先スーパー・薬局は改名しない。 |
| セカンドストリート山陽小野田店 | 運営会社の住所と店舗経路検索の座標で追加。旧ウォンツ小野田店の閉店IDは保持。 |
| 食楽 金龍 | 自店Instagramの閉店告知を確保。既存対象IDなし・後継とりいちず反映済みのため変更不要。別市のラーメン金龍を除外。 |
| メガドリ宇部店 | 公式Xで厚南中央1丁目7-48と現行案内を確認。新店舗の地図点未確保につき追加保留。 |
| 東須恵のWatts開店日 | 公式ニュースと自社PRの表記が矛盾。現行掲載は反映、実開店日は保留。 |
| 東須恵の薬局計画 | 公式ページの不掲載だけで中止と断定せず保留。 |

追加9件は全て `listed`／実開店日null。新築9建物という意味ではない。店舗名とIDを対応付けた公式マーカー、または公式店舗経路検索の目的地を使用。地図の表示中心や旧店座標は使わない。入口・館内動線・徒歩到達性は未確認と明示した。

「100円・生活雑貨」「リユース」の分類とアイコンを追加。地図・検索・徒歩候補の通常処理に載せる。入居先の薬局重複2ID（宇部亀浦）は勝手に統合していない。

## 現在の数値（単位を混ぜない）

- 施設：元7,764、既存更新24、追加154、読込7,918（履歴込み）、通常候補7,857、閉店6。
- 記事：Jev分類済み1,186、全列挙イベントの判断完了17、一部確認4、未着手1,165。
- 記録イベント：36、判断済み31、保留5。5保留はメガドリ2記事・薬局計画2記事・Watts開店日1件という**3論点**。未列挙記事があるため総イベント数は未確定。
- 今回Jev：公式店舗9対象・2リクエスト、待機合計1,669.5717ms、成功入力6,516tokens、使用量推定$0.000273672。これはAPI処理時間だけで、根拠収集・検証・保存を含む総作業時間ではない。
- Jev累計2,025対象、残975対象／保守的残予算$0.39444736。全体上限3,000対象・$1。応答を再利用する。
- JevはWatts8店を店内別売場、後継1店をnew_unmatchedに分類。後継の旧店舗との関係は人手でdifferent_prior_occupantに補正した。Jevの出力はそのまま採用しない。大規模照合の精度を9件で証明したわけではない。

## 根拠・コード・再現性

- `data-sources/facility-successors-20260925/`：対象URL、全8店舗計画、手動採否、採用9件、集計、食楽金龍の不在照合記録。
- `outputs/facility-successors-20260925/`：14取得ページ、変更前overlay、抽出店舗、Jevの固定入力・応答・課金予約記録。Git非公開。秘密のJev認証ファイルは含めない。
- `scripts/prepare-facility-successors.mjs`：公式市内一覧の全店網羅、店舗ID・名称に結びつくマーカー抽出、既存周辺候補の再現。
- `scripts/triage-facility-successors.mjs`：過去4バッチの対象数と保守的予約費用を累積。`--report` は保存応答の再集計。再開時に `--run` は不要。
- `scripts/apply-facility-successors.mjs --apply`：確認済み9件を適用。今回実測で2回目は `newly_reflected:0`、既存差分の変質は拒否。
- `scripts/check-successor-closure-absence.mjs`：既存対象ID不在・類似他店除外・告知本文を検証。
- `scripts/build-facility-progress-ledger.mjs`：記事・施設・イベント台帳を再生成。記事本文ではなくGit管理のmetadataと採否をCIで検証。
- 記事正本：`data-sources/facility-progress-20260925/article-event-reviews.json`。
- 個別の現行掲載追加を記事全体の完了と混同しない。Watts開店日の保留は残している。

## 検証済み／未完了

成功：typecheck、元施設検証、`test-facility-current.mjs`（既存属性保全、検索・候補、反映件数）、新規・台帳・移転の15テスト、同じ適用の再実行0件。

未完了：実画面のSSR検証・全build・release検証はメモリ不足で中断。全bulkテストは最終結果を回収できなかったため成功扱いにしない。最新差分はPR未反映、最新差分に対するCIも未実施。

再開指示後はまず軽量な `node --test --test-concurrency=1 scripts/test-facility-successors.mjs scripts/test-facility-progress-ledger.mjs scripts/test-facility-relocations.mjs` を必要に応じ実行。コード変更がなければ上の成功を無意味に取り直さない。既存PRに差分をpush後、GitHub CIで画面・build・releaseを完走させる。CIはpull_request検証、Pages公開は手動実行なので、公開ワークフローは触らない。

Nodeは `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`。低負荷の検証は `--max-old-space-size=128` で成功。PowerShellの起動自体がメモリ不足になる場合があり、Windows PowerShell（login:false）で軽量操作は成功した。

## 保存先と引継ぎ

正式保存先：Google Drive `Codex_非積算プロジェクト / bus-stop-compact-town`。

今回の006用引継ぎ文書と復元差分ZIPは既存の作業保存フォルダへ保存する。最終的なファイルID・SHA256・読み戻し結果は同じローカル `outputs/handoff-006-20260925/delivery-receipt.json` と新タスクの最初のメッセージを参照。

保存フォルダ：https://drive.google.com/drive/folders/10Xz7Tz3qnQrZnaG0PNL8FJzJQ5owu03h

前段復元原本：https://drive.google.com/file/d/1lk0bgqJGKw7xIvpx8XkUeyhIF3zcgCNR/view

006用ZIPは今回の差分と根拠であり、全プロジェクト原本ではない。ベースHEAD＋前段のDrive原本と組み合わせる。同じ作業場所が残っている限り展開もコピーも不要。
