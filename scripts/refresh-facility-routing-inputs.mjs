import fs from 'node:fs';import assert from 'node:assert/strict';import {hash} from './jev-batch.mjs';import {enrichRoutingInputs} from './facility-routing-lib.mjs';
const R=f=>JSON.parse(fs.readFileSync(f,'utf8')),p='data-sources/facility-workboard-20260925/article-work-inputs.json',input=R(p),queue=R('outputs/facility-bulk-triage-20260925/review-queue.json'),overlay=R('public/data/facility-current.json');
assert.equal(hash(queue),input.input_queue_hash);const updated={...input,articles:enrichRoutingInputs(input.articles,queue,overlay)};updated.note='Routing hints only. Exact official-domain links suggest shared lookup, not ownership or current status. Reviewed name/city matches suggest evidence reuse, not store identity. No article or map decisions are changed.';
fs.writeFileSync(p,JSON.stringify(updated,null,2)+'\n');console.log({articles:updated.articles.length,with_operator_links:updated.articles.filter(a=>a.operator_link_hints.length).length,with_reviewed_hints:updated.articles.filter(a=>a.reviewed_facility_hint_ids.length).length});
