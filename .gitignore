node_modules/
dist/
work/
outputs/
.env
.env.*
!.env.example
*.log

# Python
__pycache__/
*.pyc

# 標高タイルのキャッシュ。raw_data/elevations.json に必要な値だけ抜いてあるので、
# タイル本体(500MB超)はリポジトリに置かない。ローカルには残ってよい。
raw_data/dem/

# 焼き込み成果（Releaseで配布）
public/data/walk/
# fetch-walk-data.mjs が展開中に使う作業ディレクトリ（失敗時に残ることがある）
public/data/.walk-fetch-*
